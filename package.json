{
  "homepage": "https://arturnagaycev.github.io/tailwind_inteo_layout",
  "scripts": {
    "build": "npx tailwindcss --postcss -i ./src/input.css -o ./dist/style.css",
    "build-watch": "npm run build -- --watch",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "postcss": "^8.4.21",
    "postcss-import": "^15.1.0",
    "tailwindcss": "^3.2.7"
  }
}
